# 复制为项目根目录 keystore.properties，并生成 signing/upload.jks（勿提交到 Git）
#
# 本机生成签名（仅需一次，请自行备份 signing/upload.jks）：
#   mkdir signing
#   keytool -genkey -v -keystore signing/upload.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000
#
# GitHub Actions 需配置 Secrets：
#   UPLOAD_KEYSTORE_BASE64  —  upload.jks 的 base64
#   UPLOAD_STORE_PASSWORD
#   UPLOAD_KEY_ALIAS        —  与 keytool 的 alias 一致，一般为 upload
#   （upload.jks 为 PKCS12，私钥口令与 storePassword 相同；不必单独配 UPLOAD_KEY_PASSWORD）
#
# PowerShell 生成 BASE64（必须一整行，Secret 里不要换行）：
#   [Convert]::ToBase64String([IO.File]::ReadAllBytes("signing/upload.jks")) | Set-Clipboard
# 本机验证密码是否正确：
#   keytool -list -keystore signing/upload.jks -alias upload

storeFile=signing/upload.jks
storePassword=
keyAlias=upload
# PKCS12：与 storePassword 相同（Gradle 会忽略不同值并改用 storePassword）
keyPassword=

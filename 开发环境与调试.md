# 成电教务助手 — 开发环境与调试说明

本文档总结当前 **UESTC-EAMS-Helper** 安卓工程（仓库根目录 `/EAMS`）使用的构建环境、主要依赖库，以及团队实际采用的调试与安装路线。

> 应用包名：`com.milloong.uestc.Helper`
> Kotlin 命名空间：`edu.uestc.eams.helper`
> 工程名：`UESTC-EAMS-Helper`（Gradle `rootProject.name`）

---

## 1. 开发环境

### 1.1 操作系统与工具链

| 项目 | 说明 |
|------|------|
| **代码目录** | WSL/Linux：`/home/saverm/EAMS`（亦可在 Windows 侧通过 `/mnt/c/...` 访问） |
| **JDK** | **17+**（`compileOptions` / Kotlin `jvmTarget = 17`） |
| **Gradle** | **9.3.1**（Wrapper，`gradle/wrapper/gradle-wrapper.properties`） |
| **Android Gradle Plugin** | **9.1.1** |
| **Kotlin** | **2.0.21**（含 Compose Compiler 插件 `org.jetbrains.kotlin.plugin.compose`） |
| **Android SDK** | `compileSdk 36`（minor API 1），`targetSdk 36`，`minSdk 24` |
| **IDE** | 推荐 **Android Studio** 最新稳定版；命令行可用 `./gradlew` |

### 1.2 Gradle 与仓库

- 依赖版本集中管理：`gradle/libs.versions.toml`
- 插件与模块：`settings.gradle.kts`（仅 `:app` 模块）
- Maven 仓库：`google()`、`mavenCentral()`、`jitpack.io`
- JVM 参数：`org.gradle.jvmargs=-Xmx2048m`（见 `gradle.properties`）
- 本地 SDK 路径：由 `local.properties` 的 `sdk.dir` 指定（**勿提交 Git**）

### 1.3 签名与构建类型

| 构建类型 | 说明 |
|----------|------|
| **debug** | 日常开发；若存在 `keystore.properties` 则使用 upload 证书签名 |
| **release** | 根工程默认 **禁用** `assembleRelease` 等任务；需自行配置后再启用 |

- 签名配置示例：`keystore.properties.example` → 复制为 `keystore.properties`（勿提交）
- Debug 构建完成后会自动复制 APK：`app/build/outputs/apk/debug/UESTC-EAMS-Helper.apk`

### 1.4 当前应用版本（以 `app/build.gradle.kts` 为准）

- `versionName`：**1.2.3**
- `versionCode`：**33**

---

## 2. 主要依赖库

版本号来自 `gradle/libs.versions.toml`；Compose 库版本由 BOM 统一管理。

### 2.1 UI 与 Compose

| 库 | 版本 | 用途 |
|----|------|------|
| **Compose BOM** | `2024.10.01` | Compose 套件版本对齐 |
| **compose-ui** | BOM | 基础 UI |
| **compose-foundation** | BOM | 布局、手势、滚动等 |
| **compose-material3** | BOM | Material 3 组件 |
| **compose-material-icons-extended** | BOM | 扩展图标 |
| **activity-compose** | `1.13.0` | Compose Activity 集成 |
| **lifecycle-viewmodel-compose** | `2.8.7` | ViewModel + Compose |
| **lifecycle-runtime-compose** | `2.8.7` | 生命周期 + Compose |
| **kizitonwose calendar-compose** | `2.6.2` | 月历组件（开学周对齐等） |
| **sheets-compose-dialogs** (core + input) | `1.3.0` | 登录弹窗等输入对话框 |

### 2.2 传统 Android 视图（部分页面仍使用）

| 库 | 版本 | 用途 |
|----|------|------|
| **appcompat** | `1.7.1` | 兼容库 |
| **material** | `1.13.0` | Material Design 组件 |
| **activity** | `1.13.0` | Activity 基础 |
| **fragment-ktx** | `1.8.8` | Fragment（Web 内置浏览器） |
| **viewBinding** | AGP 内置 | `CourseWebActivity` / Web 页 |

### 2.3 网络、数据与后台

| 库 | 版本 | 用途 |
|----|------|------|
| **OkHttp** | `4.12.0` | HTTP 客户端、Cookie 会话 |
| **okhttp-brotli** | `4.12.0` | Brotli 解压 |
| **Gson** | `2.11.0` | JSON 解析 |
| **kotlinx-coroutines** (android + core) | `1.9.0` | 协程 |
| **lifecycle-runtime-ktx** | `2.8.7` | 生命周期 |
| **WorkManager** | `2.10.0` | 课表定时同步、上课提醒 |

### 2.4 测试

| 库 | 版本 |
|----|------|
| JUnit | `4.13.2` |
| AndroidX JUnit | `1.3.0` |
| Espresso | `3.7.0` |
| OkHttp MockWebServer | `4.12.0` |

---

## 3. 技术架构概要

```
app/src/main/java/edu/uestc/eams/helper/
├── ui/compose/          # 主界面：课表、成绩、考试、我的（Jetpack Compose）
├── ui/viewmodel/        # MainViewModel
├── ui/browser/          # Web 内置浏览器 Fragment（ViewBinding，勿随意改登录 Web 流程）
├── data/
│   ├── auth/            # CAS 统一认证登录
│   ├── eamsapp/         # 移动教务 API
│   ├── network/         # OkHttp、CookieJar
│   ├── parser/          # 课表/成绩/考试 JSON 与 WakeUp HTML 解析
│   ├── prefs/           # SharedPreferences（排版、提醒、成绩勾选等）
│   └── local/           # 本地课表缓存
├── worker/              # WorkManager 后台任务
└── notification/        # 上课通知
```

- **主 UI 栈**：Kotlin + **Jetpack Compose** + Material 3 + ViewModel
- **登录 UI**：`sheets-compose-dialogs` + `CasLoginFlow.kt`
- **Web 登录/导入会话**：`CourseWebActivity` + `CourseWebFragment`（WebView，与 Compose 主流程分离）
- **持久化**：SharedPreferences + 本地 XML 缓存；Cookie 存应用私有目录

---

## 4. 调试路线

当前实际开发采用 **「WSL 编译 + Windows adb 装真机」** 的方案（方案 B）。

### 4.1 总体流程

```
WSL 工程目录
    │
    ├─ ./gradlew :app:assembleDebug          # 编译 Debug APK
    │
    ├─ 复制 APK 到 Windows 下载目录
    │     /mnt/c/Users/123/Downloads/UESTC-EAMS-Helper-debug.apk
    │
    └─ PowerShell 调用 Windows adb 安装
          C:\Users\123\platform-tools\adb.exe -P 5038 install -r -g ...
```

### 4.2 一键脚本（推荐）

在 WSL 工程根目录执行：

```bash
bash scripts/install-phone-debug.sh
```

脚本会依次：

1. `./gradlew :app:assembleDebug`
2. 复制 APK 到 `C:\Users\123\Downloads\UESTC-EAMS-Helper-debug.apk`
3. 调用 `scripts/install-phone-debug.ps1` 通过 Windows adb 安装

### 4.3 Windows adb 配置

| 配置项 | 当前值 |
|--------|--------|
| **adb 路径** | `C:\Users\123\platform-tools\adb.exe` |
| **adb 端口** | **5038**（`-P 5038`，避免与默认 5037 冲突） |
| **APK 路径** | `C:\Users\123\Downloads\UESTC-EAMS-Helper-debug.apk` |
| **测试设备** | vivo **V2339A**（USB 连接，`adb devices -l` 可见） |

安装前脚本会尝试关闭安装校验（便于 debug 反复覆盖安装）：

```powershell
adb shell settings put global verifier_verify_adb_installs 0
adb shell settings put global package_verifier_enable 0
adb shell settings put global upload_apk_enable 0
```

安装命令：`adb -P 5038 install -r -g <apk>`（覆盖安装并授予 manifest 权限）。

### 4.4 常见安装问题

| 现象 | 处理 |
|------|------|
| **签名不一致** | 首次换证书时需先卸载旧包再装；之后用 `install -r` 即可 |
| **adb 找不到设备** | 确认 USB 调试、Windows 上 `adb -P 5038 devices` 有 device |
| **WSL 直连 adb 不可用** | 本工程故意走 **Windows adb**，不要只在 WSL 内装 platform-tools Unless 另行配置 |

### 4.5 仅编译、不安装

```bash
./gradlew :app:assembleDebug
# 产物：
#   app/build/outputs/apk/debug/app-debug.apk
#   app/build/outputs/apk/debug/UESTC-EAMS-Helper.apk  （assembleDebug 自动复制）
```

其他常用 Gradle 任务：

```bash
./gradlew :app:compileDebugKotlin     # 仅编译 Kotlin，较快
./gradlew :app:installDebug           # 需要本机 adb 可用时直接安装
./gradlew :app:installDebugKeepSession # 覆盖安装说明任务（依赖 installDebug）
```

### 4.6 模拟器与会话备份（可选）

若使用 **Android 模拟器** 调试登录态，可用 Gradle 任务备份/恢复 SharedPreferences（免重复登录）：

```bash
./gradlew :app:backupEmulatorSession   # 备份到 .local/emulator-session/
./gradlew :app:restoreEmulatorSession  # 卸载重装后写回模拟器
```

备份文件包括：

- `uestc_okhttp_cookie_snapshot_v1.xml`（Cookie 会话）
- `academic_cache.xml`（课表等缓存）

> 含学号/Cookie 的备份目录 **勿提交 Git**。

### 4.7 日志与排错

**真机 logcat（经 Windows adb）：**

```bash
powershell.exe -NoProfile -Command "& 'C:\Users\123\platform-tools\adb.exe' -P 5038 logcat -d | Select-String 'AndroidRuntime|eams.helper'"
```

**查看崩溃栈：**

```bash
powershell.exe -NoProfile -Command "& 'C:\Users\123\platform-tools\adb.exe' -P 5038 logcat -d *:E"
```

**Android Studio：**

- Run → 选择设备 → Debug
- Logcat 过滤：`com.milloong.uestc.Helper`

### 4.8 开发约定（当前会话内约定）

1. **不要随意修改** Web 登录相关：`CourseWebActivity` / `CourseWebFragment`（除非明确要求改 Web 登录流程）。
2. **真机安装**：使用 `scripts/install-phone-debug.sh`；仅在需要真机验证时执行，避免频繁打扰。
3. **课表排版设置** 持久化在 `TimetableDisplayPreferences`（字号、行高、列宽、中午分界线、课程名精简/完整等）。
4. **Compose 兼容性**：避免使用与运行时 Compose 版本不匹配的 API（例如曾导致「我的」页崩溃的 `FlowRow` 新签名问题，已改为 `Row`）。

---

## 5. 与 README 的关系

- **用户向说明**（功能、使用、Fork 指南）：见根目录 [`README.md`](README.md)
- **本文档**：面向开发者，记录 **环境、依赖、构建与真机调试路径**
- **Python 命令行版**：同级目录 `UESTC-EAMS-Helper-Python/`（独立仓库镜像）
- **旧 App 目录**：`UESTC-EAMS-Helper-App/` 为历史/镜像副本，**以根目录 `app/` 模块为准**

---

## 6. 快速检查清单

开发前确认：

- [ ] JDK 17+、`local.properties` 中 SDK 路径正确
- [ ] `./gradlew :app:compileDebugKotlin` 通过
- [ ] 真机调试：USB 连接，`adb -P 5038 devices` 可见设备
- [ ] 需要装包时：`bash scripts/install-phone-debug.sh`

---

*文档随工程依赖与调试习惯更新；若 adb 路径、端口或 APK 输出目录变更，请同步修改 `scripts/install-phone-debug.*` 与本文件。*
